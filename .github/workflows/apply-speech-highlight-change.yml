name: apply-speech-highlight-change

on:
  push:
    branches:
      - feature/speech-word-highlighting

jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: echo "workflow-ok"
